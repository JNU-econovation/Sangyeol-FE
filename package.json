{
  "name": "sangyeol",
  "packageManager": "yarn@4.18.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "!packages/app-legacy"
  ],
  "scripts": {
    "postinstall": "yarn workspace @sangyeol/utils build && yarn workspace api build",
    "dev:app": "yarn workspace app start",
    "dev:web": "yarn workspace web dev",
    "build:web": "yarn workspace @sangyeol/utils build && yarn workspace api build && yarn workspace web build",
    "start:web": "yarn workspace web start",
    "build:app-ios": "yarn workspace @sangyeol/utils build && yarn workspace api build && yarn workspace app ios",
    "build:app-android": "yarn workspace @sangyeol/utils build && yarn workspace api build && yarn workspace app android",
    "build:app": "yarn workspace @sangyeol/utils build && yarn workspace api build && yarn workspace app ios && yarn workspace app android",
    "build:utils": "yarn workspace @sangyeol/utils build",
    "build:api": "yarn workspace api build",
    "type-check": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.0.0",
    "@eslint/js": "^9.0.0",
    "@types/node": "^20.0.0",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-prettier": "^5.0.0",
    "prettier": "^3.0.0",
    "typescript": "~6.0.3",
    "typescript-eslint": "^8.0.0"
  },
  "dependencies": {
    "app": "workspace:^",
    "expo": "^57.0.4",
    "expo-dev-client": "^57.0.10",
    "workspace": "^0.0.1-preview.2"
  }
}
