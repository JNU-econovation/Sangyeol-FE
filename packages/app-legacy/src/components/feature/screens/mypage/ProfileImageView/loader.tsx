import styled from "@emotion/native";
import { COLORS } from "@styles/colorPalette";

const ProfileImageViewLoader = styled.View`
  width: 96px;
  height: 96px;
  border-radius: 48px;
  background-color: ${COLORS.gray300};
`;

export default ProfileImageViewLoader;
